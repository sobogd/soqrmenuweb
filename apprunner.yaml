version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - npm install --force
      - npm run build
run:
  runtime-version: 18
  command: node .next/standalone/server.js
  network:
    port: 3000
  env:
    - name: NODE_ENV
      value: production
    - name: PORT
      value: "3000"
